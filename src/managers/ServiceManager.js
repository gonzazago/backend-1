

/*
Estructura de un servicio { id, name, description, duration, price, category, available }
    getServices() // toda la lista de servicios
    getServiceById(id) // un servicio por id
    addService(serviceData) // agrega un servicio nuevo
    updateService(id, updatedData) // actualiza un servicio
    deleteService(id) // borra
 */